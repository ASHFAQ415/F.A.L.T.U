# ============================================================
#  Backend Dependencies — All Open Source, All Free
# ============================================================

# ── Web Framework ─────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9        # File uploads

# ── Data Validation ───────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Database (SQLite ORM) ─────────────────────────────────
sqlmodel==0.0.19               # SQLModel = SQLAlchemy + Pydantic
aiosqlite==0.20.0              # Async SQLite driver

# ── Authentication ────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # Password hashing

# ── HTTP Client (async — for Ollama calls) ────────────────
httpx==0.27.0

# ── Vector Database ───────────────────────────────────────
chromadb==0.5.3                # On-disk vector DB (completely free)

# ── Reranking (local, no API needed) ─────────────────────
sentence-transformers==3.0.1   # Cross-encoder reranking
torch==2.3.1                   # Required by sentence-transformers
                               # (CPU-only on Oracle Cloud free tier)

# ── Document Parsing ──────────────────────────────────────
pymupdf==1.24.5                # PDF text extraction (fast, reliable)
python-docx==1.1.2             # Word (.docx) text extraction
markdown==3.6                  # Markdown processing

# ── BM25 Full-Text Search (for hybrid retrieval) ──────────
rank-bm25==0.2.2

# ── Caching ───────────────────────────────────────────────
cachetools==5.3.3              # In-memory TTL cache (replaces Redis)

# ── Scheduling (replaces AWS Lambda + EventBridge) ────────
apscheduler==3.10.4            # Background scheduled jobs

# ── Monitoring / Metrics ──────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0

# ── PII Detection & Redaction ─────────────────────────────
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.5

# ── Logging ───────────────────────────────────────────────
structlog==24.2.0

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.1
numpy==1.26.4                  # Vector math
scipy==1.13.1                  # Cosine similarity for semantic cache
